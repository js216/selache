[package]
name = "selas"
version = "0.1.0"
edition = "2021"
description = "SHARC+ assembler for the Selache toolchain"

[dependencies]
selelf = { path = "../selelf" }
selinstr = { path = "../selinstr" }
