[package]
name = "selcc"
version = "0.1.0"
edition = "2021"
description = "C compiler for the Selache toolchain"

[dependencies]
selas = { path = "../selas" }
seld = { path = "../seld" }
selinstr = { path = "../selinstr" }

[dev-dependencies]
selelf = { path = "../selelf" }
